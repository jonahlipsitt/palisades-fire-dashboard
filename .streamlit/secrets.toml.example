# Streamlit Secrets Configuration Template
# Copy this to .streamlit/secrets.toml and fill in your values
# DO NOT commit the actual secrets.toml file to Git!

[gee]
project_id = "your-gee-project-id"
# For production, you might also need service account credentials
# service_account_info = """{"type": "service_account", "project_id": "...", ...}"""

# Example of how to set environment variables in Streamlit Cloud:
# GEE_PROJECT_ID = "your-gee-project-id"